---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time style="margin: 0; color: gray;" datetime={date?.toISOString()}>
	{
		date?.toLocaleDateString('ko', {
			year: 'numeric',
			month: 'numeric',
			day: 'numeric',
		})
	}
</time>